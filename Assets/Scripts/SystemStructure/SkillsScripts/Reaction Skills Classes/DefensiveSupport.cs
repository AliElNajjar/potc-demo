using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefensiveSupport : ReactionSkills
{
    /*
     *    Reactions that become available when an ally takes damage or receives a status ailment will commonly use the back forward input,
     *    or 46 on the number pad.
     *    Note, as this isn’t a fighting game, the charge (holding back) isn’t necessary.
     */
}
